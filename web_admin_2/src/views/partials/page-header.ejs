<section class="page-header">
  <div>
    <p class="eyebrow"><%= eyebrow || 'Module' %></p>
    <h2><%= title %></h2>
    <% if (description) { %>
      <p class="page-header__description"><%= description %></p>
    <% } %>
  </div>
</section>
